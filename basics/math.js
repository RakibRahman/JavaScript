export let add = function add(x, y) {
  return x + y;
};
export let subtract = function add(x, y) {
  return x - y;
};
export let multiply = function add(x, y) {
  return x * y;
};
export let divide = function add(x, y) {
  return x / y;
};
export let pi = 3.1415;
